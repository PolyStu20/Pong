//Created a new enumeration called 'GameState' with the options 'Initialising', 'Playing', and 'GameOver'. 
//We may add more in the future, but we know these are going to be used for our application.

public enum GameState {
	Initialising,
	Playing,
	GameOver,
}
